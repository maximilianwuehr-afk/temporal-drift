# Temporal Drift fixture vault

This folder is a minimal Obsidian vault used for:
- local visual captures (`scripts/visual/capture.sh`)
- local smoke checks (`scripts/e2e/smoke.sh`)

The contents are intentionally deterministic.
